pipeline {
    agent any

    tools {
        maven 'Maven_3.9'
        jdk 'JDK11'
    }

    stages {
        stage('Checkout') {
            steps {
                git branch: 'practise',
                    url: 'https://github.com/Anonymous16050/mavencode.git'
            }
        }

        stage('Build') {
            steps {
                dir('Hello') {
                    bat "mvn clean package"
                }
            }
        }

        stage('Test') {
            steps {
                dir('Hello') {
                    bat "mvn test"
                }
            }
            post {
                always {
                    junit 'Hello/target/surefire-reports/*.xml'
                }
            }
        }

        stage('Deploy') {
            steps {
                dir('Hello') {
                    script {
                        // Correct double % for Jenkins bat
                        def jarFile = bat(
                            script: 'for %%i in (target\\*.jar) do @echo %%~nxi',
                            returnStdout: true
                        ).trim()

                        echo "Deploying JAR: ${jarFile}"

                        // Run the JAR
                        bat "start java -jar target\\${jarFile}"
                    }
                }
            }
        }
    }

    post {
        always {
            mail to: 'rajkumarjittaaboyini@gmail.com',
                 subject: "Build ${currentBuild.currentResult}: Job '${env.JOB_NAME} [${env.BUILD_NUMBER}]'",
                 body: "Check console output at ${env.BUILD_URL}"
        }
    }
}
